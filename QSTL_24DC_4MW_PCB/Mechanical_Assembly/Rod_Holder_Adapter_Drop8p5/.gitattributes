# Retain exact native/exported CAD and delivery bytes.
*.dwg binary
*.idw binary
# Native IPN embeds BMP thumbnails; store its exact bytes through Git LFS.
*.ipn filter=lfs diff=lfs merge=lfs -text
*.pdf binary
*.zip binary
*.json -text whitespace=cr-at-eol
